--- a/x86_64-link.c
+++ b/x86_64-link.c
@@ -12,7 +12,11 @@
 
 #define R_NUM       R_X86_64_NUM
 
+#ifdef TARGETOS_sic
+#define ELF_START_ADDR 0x8000000000 /* sic user space starts above the kernel's 4 GiB identity map */
+#else
 #define ELF_START_ADDR 0x400000
+#endif
 #define ELF_PAGE_SIZE  0x1000
 
 #define PCRELATIVE_DLLPLT 1
